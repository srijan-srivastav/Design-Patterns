package DesignPatterns.StrategyPattern.Processors;

public class A1BionicProcessor implements IProcessor {
    @Override
    public String processor() {
        return "Use the A1 bionic processor";
    }
}
